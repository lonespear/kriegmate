# Lower bounds for installation. The exact versions the committed results in runs/ were
# produced with are in requirements-lock.txt (see docs/technical-reference.md, Reproducibility).
streamlit>=1.50
pydeck>=0.9
altair>=5
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pandas>=2.0
matplotlib>=3.8
requests>=2.31
pillow>=10
pytest>=8
